# Pasta de imagens

Coloque aqui as imagens que o site usa. Nomes sugeridos (usados em `index.html`):

- `capa.jpg` — imagem da capa / hero
- `foto1.jpg` — foto especial (usada na seção de imagem)
- `foto2.jpg` — foto adicional
- `` — foto adicional
- `background.jpg` — imagem de fundo (opcional)

Você pode usar qualquer extensão comum (`jpg`, `png`, `webp`). Substitua os nomes no `index.html` se preferir outros arquivos.
